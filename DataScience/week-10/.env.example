# On Linux, set this to your host user ID so the container can write to ./airflow/logs and ./airflow/dags:
#   echo "AIRFLOW_UID=$(id -u)" > .env
# Otherwise leave as 50000 (default Airflow user in the image).
AIRFLOW_UID=50000
